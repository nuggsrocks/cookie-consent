{
  "name": "cookie-consent",
  "version": "1.2.0",
  "type": "module",
  "main": "./dist/consent.iife.js",
  "scripts": {
    "build": "rm -rf dist && esbuild index.ts --bundle --minify --format=iife --loader:.css=text --outfile=dist/consent.iife.js"
  },
  "packageManager": "pnpm@10.28.1",
  "devDependencies": {
    "@types/node": "^25.2.0",
    "esbuild": "^0.27.2",
    "typescript": "^5.9.3"
  }
}
